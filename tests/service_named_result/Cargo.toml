[package]
name = "service_named_result"
version = "0.1.0"
edition = "2021"
publish = false

[features]
current-thread = []
default = []

[dependencies]
prost = "0.12"
tonic = {path = "../../tonic"}

[build-dependencies]
tonic-build = {path = "../../tonic-build"}
